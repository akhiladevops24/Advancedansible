---
- name: install tomcat on ubuntu
  hosts: appserver
  become: yes
  tasks:
   - name: install java
     apt:
       name: openjdk-8-jdk
       update_cache: yes
       state: present
     when: ansibile_facts['distribution'] == "Ubuntu" 
     
